import React from 'react';
import MapViewNative, { Marker as MarkerNative } from 'react-native-maps';

export const MapView = MapViewNative;
export const Marker = MarkerNative;
export default MapViewNative;
